apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "workerra-ci.fullname" . }}-test-connection"
  labels:
    {{- include "workerra-ci.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "workerra-ci.fullname" . }}:{{ .Values.svcAccessPort }}']
  restartPolicy: Never
